{
  "name": "@langgraph-glove/tests-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end tests for langgraph-glove tool servers. Requires a running local instance.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:imap": "vitest run src/imap"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.2"
  }
}
